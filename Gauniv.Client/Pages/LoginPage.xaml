<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Gauniv.Client.ViewModel"
             x:Class="Gauniv.Client.Pages.LoginPage"
             Title="Connexion"
             BindingContext="{Binding Source={RelativeSource Self}}">

    <StackLayout Padding="20">
        <Label Text="Email" />
        <Entry Text="{Binding Login.Email}" Placeholder="Entrez votre email" Keyboard="Email" />

        <Label Text="Mot de passe" />
        <Entry Text="{Binding Login.Password}" Placeholder="Entrez votre mot de passe" IsPassword="True" />

        

    </StackLayout>
</ContentPage>
